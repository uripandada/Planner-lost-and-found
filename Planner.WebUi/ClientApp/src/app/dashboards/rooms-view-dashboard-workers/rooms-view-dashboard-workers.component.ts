import { Component, Input, OnInit } from '@angular/core';
import { RoomViewDashboardBedItem, RoomViewDashboardRoomItem } from 'src/app/core/autogenerated-clients/api-client';

@Component({
  selector: 'app-rooms-view-dashboard-workers',
  templateUrl: './rooms-view-dashboard-workers.component.html',
})
export class RoomsViewDashboardWorkersComponent implements OnInit {

  @Input() room: RoomViewDashboardRoomItem | RoomViewDashboardBedItem;

  constructor(
  ) {
  }

  ngOnInit() {
  }
}
