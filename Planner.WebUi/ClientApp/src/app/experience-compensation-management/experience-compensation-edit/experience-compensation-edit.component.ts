import { Component, OnInit, Input, Output, EventEmitter, OnChanges, SimpleChanges } from '@angular/core';
import { FormBuilder, FormGroup, ValidationErrors, Validators } from '@angular/forms';
import { Router } from '@angular/router';
import { ToastrService } from 'ngx-toastr';
import { BehaviorSubject } from 'rxjs';
import { InsertLostAndFoundCategoryCommand, ProcessResponse, ProcessResponseOfGuid, LostAndFoundCategoryDetailsViewModel, LostAndFoundCategoryManagementClient, UpdateLostAndFoundCategoryCommand } from '../../core/autogenerated-clients/api-client';
import { LoadingService } from '../../core/services/loading.service';

@Component({
  selector: 'app-experience-compensation-edit',
  templateUrl: './experience-compensation-edit.component.html',
  styleUrls: ['./experience-compensation-edit.component.scss']
})
export class ExperienceCompensationEditComponent implements OnInit, OnChanges {

  @Input() category: LostAndFoundCategoryDetailsViewModel;

  @Output() deleted: EventEmitter<string> = new EventEmitter<string>(); 
  @Output() inserted: EventEmitter<LostAndFoundCategoryDetailsViewModel> = new EventEmitter<LostAndFoundCategoryDetailsViewModel>();
  @Output() updated: EventEmitter<LostAndFoundCategoryDetailsViewModel> = new EventEmitter<LostAndFoundCategoryDetailsViewModel>();
  @Output() cancelled: EventEmitter<boolean> = new EventEmitter<boolean>();

  isSaving$: BehaviorSubject<boolean> = new BehaviorSubject<boolean>(false);
  isCreateNew$: BehaviorSubject<boolean> = new BehaviorSubject<boolean>(false);

  categoryForm: FormGroup;
  loading: LoadingService;

  constructor(private _router: Router, private _toastr: ToastrService, private _formBuilder: FormBuilder,
    private _CategoryManagementClient: LostAndFoundCategoryManagementClient,) {
    this.loading = new LoadingService();
  }

  ngOnInit(): void {
    this._setCreateNewStatus();

    this.categoryForm = this._formBuilder.group({
      name: [this.category.name, [Validators.required]],
      expirationDays: [this.category.expirationDays, [Validators.required]],
    });
  }

  ngOnChanges(changes: SimpleChanges): void {
    if (changes.category && !changes.category.firstChange) {
      this._setCreateNewStatus(); 
      this._setCategoryFormData();
    }
  }

  cancel() {
    this.cancelled.next(true);
  }

  private _setCreateNewStatus() {
    if (this.category.id) {
      this.isCreateNew$.next(false);
    }
    else {
      this.isCreateNew$.next(true);
    }
  }

  private _setCategoryFormData() {
    this.categoryForm.controls.name.setValue(this.category.name);
    this.categoryForm.controls.expirationDays.setValue(this.category.expirationDays);

    this.categoryForm.updateValueAndValidity({ onlySelf: false });

    this.categoryForm.markAsUntouched({ onlySelf: false });
    this.categoryForm.markAsPristine({ onlySelf: false });
  }

  save() {
    if (!this.categoryForm.valid) {
      this.categoryForm.markAllAsTouched();
      this.categoryForm.markAsDirty({ onlySelf: false });
      this._toastr.error("You have to fix form errors before you can continue.");
      return;
    }

    let formValues = this.categoryForm.getRawValue();

    if (this.isCreateNew$.value) {
      this._insertCategory();
    }
    else {
      this._updateCategory();
    }
  }

  private _insertCategory() {
    let formValues = this.categoryForm.getRawValue();
    let insertRequest: InsertLostAndFoundCategoryCommand = new InsertLostAndFoundCategoryCommand({
      name: formValues.name,
      expirationDays: formValues.expirationDays
    });

    this.loading.start();

    this.isSaving$.next(true);

    this._CategoryManagementClient.insertLostAndFoundCategory(insertRequest).subscribe(
      (response: ProcessResponseOfGuid) => {
        if (response.hasError) {
          this._toastr.error(response.message);
          this._setFormValidationErrors(response, this.categoryForm);
          this.isSaving$.next(false);
          return;
        }

        this.isSaving$.next(false);

        let CategoryDetails = new LostAndFoundCategoryDetailsViewModel({
          id: response.data,
          name: insertRequest.name,
          expirationDays: insertRequest.expirationDays
        });

        this.inserted.next(CategoryDetails);
      },
      (error: Error) => {
        this._toastr.error(error.message);
        this.isSaving$.next(false);
      },
      () => {
        this.loading.stop();
        this.isSaving$.next(false);
      },
    );
  }
  private _updateCategory() {
    let formValues = this.categoryForm.getRawValue();
    let updateRequest: UpdateLostAndFoundCategoryCommand = new UpdateLostAndFoundCategoryCommand({
      id: this.category.id,
      name: formValues.name,
      expirationDays: formValues.expirationDays,
    });

    this.loading.start();
    this.isSaving$.next(true);

    this._CategoryManagementClient.updateLostAndFoundCategory(updateRequest).subscribe(
      (response: ProcessResponse) => {
        if (response.hasError) {
          this._toastr.error(response.message);
          this._setFormValidationErrors(response, this.categoryForm);
          this.isSaving$.next(false);
          return;
        }

        this._toastr.success(response.message);
        this.isSaving$.next(false);

        let CategoryDetails = new LostAndFoundCategoryDetailsViewModel({
          id: updateRequest.id,
          name: updateRequest.name,
          expirationDays: updateRequest.expirationDays
        });

        this.updated.next(CategoryDetails);
      },
      (error: Error) => {
        this._toastr.error(error.message);
        this.isSaving$.next(false);
      },
      () => {
        this.loading.stop();
        this.isSaving$.next(false);
      },
    );
  }
  private _setFormValidationErrors(response: ProcessResponse, form: FormGroup) {
    for (let error of response.modelErrors) {
      let control = form.get(error.key);

      if (control) {
        var errors: ValidationErrors = {};
        errors[error.validatorKey] = true;

        control.setErrors(errors)
        control.markAsDirty();
        control.markAsTouched();
      }
    }
  }
}
