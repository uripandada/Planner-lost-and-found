import { Component, Input, OnInit } from '@angular/core';
import { TaskPreviewData } from '../../core/autogenerated-clients/api-client';

@Component({
  selector: 'app-tasks-preview-list',
  templateUrl: './tasks-preview-list.component.html',
  styleUrls: ['./tasks-preview-list.component.scss']
})
export class TasksPreviewListComponent implements OnInit {
  @Input() tasks: Array<TaskPreviewData> = [];

  constructor() {
  }

  ngOnInit(): void {
  }
}
