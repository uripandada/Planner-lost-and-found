import { Component, Input, OnInit } from '@angular/core';
import { FormArray, FormBuilder, FormGroup, Validators } from '@angular/forms';
import { EnumData } from '../../core/autogenerated-clients/api-client';

@Component({
  selector: 'app-task-recurring-every',
  templateUrl: './task-recurring-every.component.html',
  styleUrls: ['./task-recurring-every.component.scss']
})
export class TaskRecurringEveryComponent implements OnInit {
  @Input() taskRecurringEveryForm: FormGroup;

  constructor(private _formBuilder: FormBuilder) {
  }

  ngOnInit(): void {
  }

  //addStartsAtMonthDay() {
  //  this.startsAtMonthDaysArray.push(
  //    this._formBuilder.group({
  //      id: null,
  //      nthOfMonth: [1, Validators.required],
  //      startsAtTime: ["08:00", Validators.required]
  //    }));
  //}
}
