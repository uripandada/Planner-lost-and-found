import { Component, Input, OnInit } from '@angular/core';
import { FormGroup } from '@angular/forms';
import { EnumData } from '../../core/autogenerated-clients/api-client';

@Component({
  selector: 'app-task-recurring',
  templateUrl: './task-recurring.component.html',
  styleUrls: ['./task-recurring.component.scss']
})
export class TaskRecurringComponent implements OnInit {
  @Input() taskRecurringForm: FormGroup;
  @Input() allRecurringTaskTypes: Array<EnumData>;

  constructor() {
  }

  ngOnInit(): void {
  }
}
