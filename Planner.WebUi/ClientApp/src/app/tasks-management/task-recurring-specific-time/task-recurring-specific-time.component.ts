import { Component, Input, OnInit } from '@angular/core';
import { FormGroup } from '@angular/forms';
import { EnumData } from '../../core/autogenerated-clients/api-client';

@Component({
  selector: 'app-task-recurring-specific-time',
  templateUrl: './task-recurring-specific-time.component.html',
  styleUrls: ['./task-recurring-specific-time.component.scss']
})
export class TaskRecurringSpecificTimeComponent implements OnInit {
  @Input() taskRecurringSpecificTimeForm: FormGroup;

  constructor() {
  }

  ngOnInit(): void {
  }
}
